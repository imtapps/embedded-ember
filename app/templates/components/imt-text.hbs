This is an example embedded Ember app.

The server got the list of files from the S3 bucket and returned those to the browser.

The browser then got the files from S3 and started the Ember application once everything was ready.

Below is an example of a computed property from an Ember component and an image the is correctly referenced after finger printed.

(c) {{year}} by IMT <img src="/assets/images/imt.png">
{{yield}}
